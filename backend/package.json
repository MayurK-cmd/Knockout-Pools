{
  "name": "knockout-pools-backend",
  "version": "0.1.0",
  "description": "Knockout Pools backend (Express + ethers event listener + in-memory cache)",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "express": "^4.21.2"
  }
}
